import {heading} from './layout.mjs';
import {facts,speciesCard,photoIds} from './components.mjs';
import {escape as e,titleCase} from '../lib/html.mjs';
export function profile(s,species,photos){const photo=photos.find(p=>p.species===s.name);const groupLink=['nijsseni','macmasteri'].includes(s.group)?`/groups/${s.group}/`:'/groups/';return `${heading('Species field notes · '+s.common,s.name,s.sections.overview)}<section class="wrap"><div class="profile-banner ${photo?'':'text-only'}">${photo?`<figure><img src="/assets/${s.id}.webp" alt="${e(s.name+' — '+photo.description)}" width="1500" height="1000"><figcaption><a href="/credits/">${e(photo.author)} · ${photo.license}</a> · Aquarium form</figcaption></figure>`:''}<aside><p class="eyebrow">AT A GLANCE</p><h2>${e(s.common)}</h2><p>${e(s.region)}</p><a href="${groupLink}">${titleCase(s.group)} group ↗</a><p class="small">Trade labels: ${s.aliases.map(e).join(', ')}.</p><a href="/comparisons/" class="button outline">Compare this species</a></aside></div>${facts(s)}<p class="micro">¹ Standard length (SL), excluding the tail. Sizes are approximate. Water ranges and space recommendations are editorial aquarium planning targets; confirm origin and accustomed conditions with your supplier.</p></section><div class="wrap article-layout"><nav class="on-this-page" aria-label="On this page"><p class="eyebrow">IN THIS PROFILE</p>${Object.keys(s.sections).filter(k=>k!=='overview').map(k=>`<a href="#${k}">${({identification:'Identification',habitat:'Natural habitat',care:'Aquarium care',diet:'Feeding',breeding:'Breeding',tankmates:'Tankmates',provenance:'Origin & records'})[k]}</a>`).join('')}<a href="#references">Sources & notes</a></nav><article class="prose">${Object.entries(s.sections).filter(([k])=>k!=='overview').map(([k,p])=>`<section id="${k}"><h2>${({identification:'Know what you’re looking at',habitat:'From river to aquarium',care:'A practical aquarium plan',diet:'Feeding with observation',breeding:'When breeding changes the balance',tankmates:'Choose the company carefully',provenance:'Keep the story with the fish'})[k]}</h2><p>${e(p)}</p></section>`).join('')}<section id="references"><h2>Sources & editorial notes</h2><p>Source-checked ${s.reviewed}. Species facts draw on the references below; aquarium targets, difficulty ratings and planning advice are our editorial synthesis. Targets are deliberately narrower than some reported field ranges and are not a guarantee for every locality or strain.</p><ul>${s.sources.map(r=>`<li><a href="${e(r.url)}">${e(r.label)}</a></li>`).join('')}</ul><p>Informal groups and complexes are navigation aids, not fixed taxonomic ranks. <a href="/about/">Read the editorial approach.</a></p></section><aside class="availability"><p class="eyebrow">WHEN YOU’RE READY</p><h2>Check availability in our aquarium network.</h2><p>ApistoPalace is part of the PetZoneSD network. These links lead to related retailers; availability, identification and shipping details must be confirmed there. No live inventory is connected here.</p><a href="https://apistogrammarama.com/">Visit Apistogrammarama ↗</a><a href="https://petzonesd.com/">Visit PetZoneSD ↗</a></aside></article></div><section class="section wrap compact-top"><p class="eyebrow">CONTINUE YOUR EXPLORATION</p><h2>Worth a closer look.</h2><div class="cards three">${s.similar.map(id=>species.find(f=>f.id===id)).filter(Boolean).map(f=>speciesCard(f)).join('')}</div></section>`}
